version: "3"
services:
  test_postgres_db:
    image: postgres:13.7-alpine
    ports:
      - "5434:5432"
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_DB: brikllo
      POSTGRES_USER: user
